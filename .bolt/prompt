I want to convert this application into a complete Portfolio Demo Version.

IMPORTANT:

Before making any changes:

1. Analyze the entire codebase.
2. Understand all modules, pages, dashboards, reports, workflows, forms, APIs, database models, authentication flows, and business logic.
3. Identify what the system currently does.
4. Implement the demo functionality based on the existing application structure.
5. Run the application locally and verify everything works correctly.
6. Fix any build errors, runtime errors, deployment issues, missing dependencies, or broken functionality encountered during the process.

DEMO LOGIN REQUIREMENTS

1. Add a prominent "Demo Login" / "Try Demo" button on the login page.

2. When a user clicks Demo Login:

   * Automatically authenticate them.
   * No signup required.
   * No email required.
   * No password required.
   * No OTP required.
   * One-click access.

3. Create a dedicated Demo User account.

4. Demo users should be able to explore the application immediately.

DEMO DATA REQUIREMENTS

1. Analyze all existing database models and application modules.

2. Automatically generate realistic demo data for every module currently present.

3. Populate:

   * Dashboards
   * Reports
   * Charts
   * Tables
   * Analytics
   * Forms
   * Records
   * Activity Logs
   * KPIs
   * Any other existing system components

4. Every page should appear fully active and production-like.

5. Do NOT use placeholder values such as:

   * Test User
   * Demo User
   * Sample Record
   * Example Data

Generate realistic records matching the application's actual purpose.

6. Add a visible banner:

🚀 DEMO MODE — All data displayed is automatically generated for demonstration purposes.

7. Add a "Refresh Demo Data" button that generates a completely new realistic dataset.

DEPLOYMENT REQUIREMENTS

After implementation:

1. Run the application locally.
2. Verify all pages load successfully.
3. Verify Demo Login works.
4. Verify all demo data loads correctly.
5. Verify no runtime errors exist.
6. Verify no build errors exist.
7. Verify production build completes successfully.

GITHUB REQUIREMENTS

1. Create a NEW GitHub repository specifically for the demo version.
2. Push all updated code to the new repository.
3. Ensure repository structure is clean and deployment-ready.
4. Include an updated README explaining:

   * Project Overview
   * Features
   * Demo Login
   * Technology Stack
   * Deployment Instructions

VERCEL DEPLOYMENT REQUIREMENTS

1. Deploy the project to Vercel.
2. Configure all required environment variables.
3. Fix any deployment issues.
4. Ensure the production deployment works correctly.
5. Verify the live URL is accessible.
6. Verify Demo Login works on the live deployment.
7. Verify demo data loads properly on the live deployment.
8. Optimize for performance and responsiveness.

FINAL DELIVERABLES

Provide:

* Summary of changes made
* Demo Login credentials (if any)
* GitHub repository URL
* Live Vercel URL
* Deployment configuration changes
* Any assumptions made
* Any remaining recommendations

Do not stop after generating code. Execute the complete implementation workflow, test it, validate it, push it to a new GitHub repository, deploy it to Vercel, and provide the final live URLs and deployment details.
